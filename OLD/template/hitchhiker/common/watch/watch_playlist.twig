{##
  # Watch playlists master builder
  #
  # This is only used if yt.page.playlist is set, which should
  # only be the case on the watch page, and of course, only
  # if a playlist renderer is present. Due to the player structure,
  # this is separate from the rest of the watch page.
  #
  # @author Nightlinbit (Daylin Cooper)
  # @version 1.0
  # @license CC0
  #}

{% include '/common/watch/watch8/playlist/appbar_playlist.twig' %}