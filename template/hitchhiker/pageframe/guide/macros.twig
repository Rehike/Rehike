{% macro channel(item) %}
{% if item.guideEntryRenderer %}
{% set this = item.guideEntryRenderer %}
<li id="{{ this.navigationEndpoint.browseEndpoint.browseId }}-guide-item" class="guide-channel guide-notification-item overflowable-list-item" role="menuitem">
    <a href="{{ this.navigationEndpoint.commandMetadata.webCommandMetadata.url }}" class="guide-item yt-uix-sessionlink yt-valign spf-link" title="{{ getText(this.formattedTitle) }}" data-external-id="{{ this.navigationEndpoint.browseEndpoint.browseId }}" data-serialized-endpoint="0qDduQEaEhh{{ this.navigationEndpoint.browseEndpoint.browseId|base64_encode() }}%3D">
        <span class="yt-valign-container">
            <span class="thumb">
                <span class="video-thumb yt-thumb yt-thumb-20">
                    <span class="yt-thumb-square">
                        <span class="yt-thumb-clip">
                            <img src="{{ this.thumbnail.thumbnails[0].url }}" alt="" onload=";window.__ytRIL && __ytRIL(this)" data-ytimg="1" width="20" height="20">
                            <span class="vertical-align"></span>
                        </span>
                    </span>
                </span>
            </span>
            <span class="display-name no-count">
                <span>{{ getText(this.formattedTitle) }}</span>
            </span>
        </span>
    </a>
</li>
{% endif %}
{% endmacro %}